Command line arguments for:
    IP
    PORT
    Enable/Disable network

Main loop
    Check if network is enabled
        inf loop
            listen for remote connection
            if remote connections
                send live video to client
                receive input from client
            else
                run robot locally
    
    If network disabled
        run robot locally